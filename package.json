}
"scripts": {
  "build": "mkdir -p dist && cp index.html sovereign-empire/index.html && tailwindcss -i ./input.css -o ./dist/style.css --minify && esbuild ./irys-bridge.js --bundle --minify --outfile=./sovereign-empire/irys-bridge.min.js"
  },
  "dependencies": {
    "@irys/web-upload": "^0.0.3",
    "@irys/web-upload-ethereum": "^0.0.3",
    "@irys/web-upload-ethereum-ethers-v6": "^0.0.3",
    "ethers": "^6.13.4"
  },
  "devDependencies": {
    "esbuild": "^0.20.2",
    "tailwindcss": "^3.4.1"
  }
}